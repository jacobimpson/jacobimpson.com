---

---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#fff" />
    <title>
      Jacob Impson | Musician and Staff Front-End Design Engineer from Perth,
      Western Australia
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,100..900;1,9..144,100..900&family=Inter:wght@400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="icon"
      href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🙂</text></svg>"
    />
  </head>
  <body>
    <canvas id="image-fragments" class="image-fragments"></canvas>

    <!-- Mobile 3D showcase area -->
    <div class="mobile-showcase"></div>

    <section class="main">
      <header>
        <h1>Jacob Impson</h1>
        <p>Staff Front-End Design Engineer. React. Animations. Fun.</p>
      </header>

      <p>
        Howdy, I am a musician and software engineer from Perth, Western
        Australia. Currently working at
        <a rel="noopener noreferrer" target="_blank" href="https://linktr.ee/"
          >Linktree</a
        >, living my teenage dream of writing front-end code for my favourite
        pop stars. I've had the amazing opportunity to build for artists at the
        top of their game like
        <a
          rel="noopener noreferrer"
          target="_blank"
          href="https://linktr.ee/oliviarodrigo">Olivia Rodrigo</a
        >,
        <a
          rel="noopener noreferrer"
          target="_blank"
          href="https://linktr.ee/chappellroan">Chappell Roan</a
        >,
        <a
          rel="noopener noreferrer"
          target="_blank"
          href="https://linktr.ee/sabrinacarpenter">Sabrina Carpenter</a
        >,
        <a
          rel="noopener noreferrer"
          target="_blank"
          href="https://linktr.ee/billieeilish">Billie Eilish</a
        >,
        <a
          rel="noopener noreferrer"
          target="_blank"
          href="https://linktr.ee/selenagomez">Selena Gomez</a
        >, and
        <a
          rel="noopener noreferrer"
          target="_blank"
          href="https://linktr.ee/carlyraejepsen">Carly Rae Jepsen</a
        >.
      </p>
      <p>
        I've spent a good chunk of my evenings this year building a product I've
        always wanted,
        <a target="_blank" href="https://vinyl.quest">vinyl.quest</a>. Log in
        with Spotify to get personalised recommendations from local record
        stores. Pulling all of my passions together, design, front-end,
        back-end, and sick beats. Give it a try.
      </p>
      <p>
        Oh, and I built this vegan recipe app called Plantree a few years ago,
        available on
        <a
          href="https://apps.apple.com/au/app/plantree-vegan-recipes/id1455705023"
          >iOS</a
        >
        and
        <a
          style="text-decoration: line-through"
          href="https://play.google.com/store/apps/details?id=com.jacobimpson.plantree&hl=en_US&gl=US"
          >Android</a
        > (stopped paying the Play Store fees so this one went down, people still
        use it though).
      </p>
      <p>
        If you want to contact me for anything work related, hit me up on <a
          href="https://www.linkedin.com/in/jacob-impson-au/"
          target="_blank"
          rel="noopener noreferrer">LinkedIn</a
        >.
      </p>
    </section>
    <section>
      <div class="project-showcase">
        <div class="project">
          <h3>vinyl.quest</h3>
          <p>
            Discover vinyl records from local stores based on your Spotify
            listening habits.
          </p>
          <a
            href="https://vinyl.quest"
            target="_blank"
            rel="noopener noreferrer">Try it out</a
          >
        </div>
        <div class="project">
          <h3>Plantree</h3>
          <p>
            Whip up delicious vegan meals using ingredients you have on hand.
          </p>
          <a
            href="https://apps.apple.com/au/app/plantree-vegan-recipes/id1455705023"
            target="_blank"
            rel="noopener noreferrer">Download on iOS</a
          >
        </div>
      </div>
    </section>

    <p class="seo-dump">
      For the sake of SEO, here's a bit of a dump of the technologies I
      regularly use, either for work or for hobby projects.. Front end: React,
      Tailwind, Framer Motion, Webpack, Micro Front Ends, Astro, Next.js.
      Backend: Node, Express, Nest.js, Larvel, Symfony, Supabase, Postgres, AWS.
      Mobile: React Native, Flutter. Design: Figma
    </p>

    <div class="profile-container">
      <img
        src="/me.png"
        alt="Jacob Impson"
        class="profile-image"
        width="60"
        height="60"
        loading="eager"
      />
    </div>
  </body>
</html>

<style lang="scss">
  * {
    box-sizing: border-box;
  }

  body {
    font-size: 15px;
    margin: 0;
    padding: 0;
    max-width: none;
    font-family: "Inter", sans-serif;
    color: #000;
    line-height: 1.3;
    font-weight: 500;
    letter-spacing: -0.2px;
    min-height: 100vh;
    position: relative;
    z-index: 10;
  }

  /* Add background overlay for text readability */
  body::before {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(255, 255, 255, 0.85);
    z-index: -1;
    pointer-events: none;
  }

  header {
    margin-bottom: 24px;
    border-bottom: 3px solid #000;
    padding-bottom: 16px;

    h1 {
      margin: 0;
      font-weight: 900;
      font-size: 32px;
      letter-spacing: -2px;
      font-family: "Fraunces", serif;
      color: #000;
      line-height: 0.9;
      // text-transform: uppercase;
      word-wrap: break-word;
      hyphens: auto;
      filter: contrast(1.2);
      margin-bottom: 8px;
    }

    p {
      font-weight: 700;
      font-family: "JetBrains Mono", monospace;
      font-size: 10px;
      color: #000;
      letter-spacing: 1px;
      text-transform: uppercase;
      margin: 0;
      background: #000;
      color: #fff;
      padding: 4px 8px;
      display: inline-block;
      word-wrap: break-word;
      border: 2px solid #000;
    }

    @media (min-width: 480px) {
      margin-bottom: 32px;
      padding-bottom: 20px;

      h1 {
        font-size: 56px;
        letter-spacing: -3px;
        margin-bottom: 11px;
      }

      p {
        font-size: 11px;
        letter-spacing: 2px;
      }
    }

    @media (min-width: 768px) {
      margin-bottom: 40px;
      padding-bottom: 32px;

      h1 {
        font-size: 84px;
        letter-spacing: -4px;
        margin-bottom: 11px;
      }

      p {
        font-size: 12px;
      }
    }
  }

  p {
    margin: 0 0 16px 0;
    font-weight: 500;
    color: #1a1a1a;
    line-height: 1.4;
  }

  a {
    color: #000;
    text-decoration: none;
    font-weight: 700;
    border-bottom: 2px solid #000;
    transition: all 0.1s ease;
  }

  *::selection {
    background: #000;
    color: #fff;
  }

  a:hover,
  a:focus {
    text-decoration: underline;
  }

  /* Mobile 3D showcase area */
  .mobile-showcase {
    height: 100px;
    width: 100%;
    position: relative;
    display: block;
  }

  /* Main content container */
  .main {
    // max-width: 800px;
    margin: 0 auto;
    padding: 20px;
    padding-top: 0;
    margin-bottom: 32px;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
  }

  /* Ensure all sections have proper mobile padding */
  section {
    // max-width: 800px;
    margin: 0 auto;
    padding: 0 20px;
    margin-bottom: 24px;
  }

  /* Compact contact section styling */
  section:last-of-type {
    // margin-bottom: 16px;

    p {
      margin-bottom: 8px;
      font-size: 14px;
      text-align: left;
    }
  }

  @media (min-width: 480px) {
    .main {
      padding: 24px;
      padding-top: 0;
      margin-bottom: 40px;
    }

    section {
      padding: 0 24px;
      // margin-bottom: 32px;
    }
  }

  @media (min-width: 768px) {
    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      max-width: 900px;
      margin: 0 auto;
      padding: 40px;
    }

    .mobile-showcase {
      display: none;
    }

    .main {
      padding: 0;
      // margin-bottom: 32px;
    }

    section {
      padding: 0;
      // margin-bottom: 48px;
    }
  }

  .project-showcase {
    display: grid;
    grid-template-columns: 1fr;
    gap: 16px;

    @media (min-width: 480px) {
      gap: 20px;
    }

    @media (min-width: 600px) {
      grid-template-columns: 1fr 1fr;
      gap: 24px;
    }

    @media (min-width: 768px) {
      gap: 32px;
    }
  }

  .project {
    background: #fafafa;
    border: 3px solid #000;
    padding: 16px;
    position: relative;

    &::before {
      content: "";
      position: absolute;
      top: 6px;
      left: 6px;
      right: 6px;
      bottom: 6px;
      border: 1px solid #e0e0e0;
      pointer-events: none;
    }

    h3 {
      margin: 0 0 8px 0;
      font-size: 24px;
      font-weight: 900;
      font-family: "Fraunces", serif;
      color: #000;
      letter-spacing: -1px;
      line-height: 1;
      text-transform: uppercase;
      word-wrap: break-word;
      hyphens: auto;
    }

    p {
      margin-bottom: 16px;
      color: #2a2a2a;
      font-weight: 500;
      line-height: 1.4;
    }

    a {
      display: inline-block;
      background: #000;
      color: #fff;
      text-decoration: none;
      font-weight: 700;
      font-family: "JetBrains Mono", monospace;
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
      border: 3px solid #000;
      padding: 10px 12px;
      transition: all 0.1s ease;
      border-bottom: 3px solid #000;

      &:hover {
        background: #fff;
        color: #000;
        padding: 10px 12px;
      }
    }

    @media (min-width: 480px) {
      padding: 20px;

      &::before {
        top: 8px;
        left: 8px;
        right: 8px;
        bottom: 8px;
      }

      h3 {
        margin-bottom: 10px;
        font-size: 26px;
      }

      p {
        margin-bottom: 18px;
      }

      a {
        font-size: 11px;
        padding: 12px 16px;

        &:hover {
          padding: 12px 16px;
        }
      }
    }

    @media (min-width: 768px) {
      padding: 24px;

      h3 {
        margin-bottom: 12px;
        font-size: 28px;
      }

      p {
        margin-bottom: 20px;
      }
    }
  }

  .seo-dump {
    font-size: 11px;
    color: #666;
    font-family: "JetBrains Mono", monospace;
    margin-top: 24px;
    padding-top: 42px;
    // padding-left: 24px;
    // padding-right: 24px;
    border-top: 1px solid #e0e0e0;
    line-height: 1.4;
    // max-width: 800px;
    margin-left: auto;
    margin-right: auto;

    padding: 24px;
  }

  .contact {
    // max-width: 800px;
    // padding: 0 20px;
    // margin-bottom: 8px;
    // margin-top: 16px;
  }

  .contact p {
    margin: 0;
    font-size: 14px;
    text-align: left;
  }

  @media (min-width: 480px) {
    .seo-dump {
      padding-left: 0;
      padding-right: 0;
      padding-top: 42px;
    }

    // .contact {
    //   padding: 0 24px;
    //   margin-bottom: 12px;
    //   margin-top: 20px;
    // }
  }

  @media (min-width: 768px) {
    .seo-dump {
      padding-left: 0;
      padding-right: 0;
    }

    // .contact {
    //   padding: 0;
    //   margin-bottom: 16px;
    //   margin-top: 24px;
    // }
  }

  .profile-container {
    display: none;
  }

  .profile-image {
    display: none;
  }

  .profile-section {
    display: none;
  }

  .image-fragments {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    pointer-events: none;
    z-index: -2;
    cursor: crosshair;
  }

  @keyframes borderShift {
    0%,
    100% {
      background: linear-gradient(
        45deg,
        #000 0%,
        #404040 25%,
        #000 50%,
        #404040 75%,
        #000 100%
      );
    }
    50% {
      background: linear-gradient(
        45deg,
        #404040 0%,
        #000 25%,
        #404040 50%,
        #000 75%,
        #404040 100%
      );
    }
  }

  @keyframes gradientFlow {
    0%,
    100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }
</style>

<script>
  import "../scripts/three-dithered-shapes";
</script>

<script type="module">
  document.addEventListener("DOMContentLoaded", function () {
    // Initialize blob canvas
    const blobCanvas = document.querySelector(".blob-canvas");
    if (blobCanvas) {
      const blobCtx = blobCanvas.getContext("2d");
      if (blobCtx) {
        initializeBlobAnimation(blobCanvas, blobCtx);
      }
    }

    // Initialize 3D paint blobs
    const fragmentCanvas = document.getElementById("image-fragments");
    if (fragmentCanvas) {
      // The initialize3DPaintBlobs function will be available from the imported script
      if (window.initialize3DPaintBlobs) {
        window.initialize3DPaintBlobs(fragmentCanvas);
      }
    }
  });

  function initializeBlobAnimation(canvas, ctx) {
    let time = 0;
    const colors = ["#1a1a1a", "#2c2c2c", "#404040", "#555555"];

    function noise(x, y, z) {
      return Math.sin(x * 0.01 + z) * Math.cos(y * 0.01 + z) * 0.5 + 0.5;
    }

    function drawBlob() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      const centerX = canvas.width / 2;
      const centerY = canvas.height / 2;
      const baseRadius = Math.min(canvas.width, canvas.height) * 0.25;

      for (let layer = 0; layer < 3; layer++) {
        ctx.beginPath();
        const points = 12;
        const angleStep = (Math.PI * 2) / points;

        for (let i = 0; i <= points; i++) {
          const angle = i * angleStep;
          const noiseValue = noise(
            Math.cos(angle) * 100,
            Math.sin(angle) * 100,
            time + layer
          );
          const radiusVariation =
            baseRadius * (0.8 + noiseValue * 0.3 + layer * 0.1);
          const x = centerX + Math.cos(angle) * radiusVariation;
          const y = centerY + Math.sin(angle) * radiusVariation;

          if (i === 0) {
            ctx.moveTo(x, y);
          } else {
            const prevAngle = (i - 1) * angleStep;
            const prevNoiseValue = noise(
              Math.cos(prevAngle) * 100,
              Math.sin(prevAngle) * 100,
              time + layer
            );
            const prevRadiusVariation =
              baseRadius * (0.8 + prevNoiseValue * 0.3 + layer * 0.1);
            const prevX = centerX + Math.cos(prevAngle) * prevRadiusVariation;
            const prevY = centerY + Math.sin(prevAngle) * prevRadiusVariation;
            const cpX = (prevX + x) / 2 + Math.sin(time + i) * 10;
            const cpY = (prevY + y) / 2 + Math.cos(time + i) * 10;
            ctx.quadraticCurveTo(cpX, cpY, x, y);
          }
        }

        ctx.closePath();
        const color = colors[layer % colors.length];
        const alpha = 0.03 - layer * 0.008;
        ctx.fillStyle =
          color +
          Math.floor(alpha * 255)
            .toString(16)
            .padStart(2, "0");
        ctx.fill();
      }

      time += 0.003;
      requestAnimationFrame(drawBlob);
    }

    function resize() {
      const rect = canvas.getBoundingClientRect();
      canvas.width = rect.width;
      canvas.height = rect.height;
    }

    resize();
    window.addEventListener("resize", resize);
    drawBlob();
  }
</script>
